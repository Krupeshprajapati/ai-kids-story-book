# AI Kids Story Book

Personalized children's storybook generator.

## Structure

- frontend → Next.js app
- backend → FastAPI server
- ai-engine → Image/PDF processing service

## Setup

Install dependencies inside each folder:

Frontend:
npm install

Backend:
pip install -r requirements.txt

AI Engine:
npm install